#include <stdio.h>

int global_var; // initialized to 0 when in global scope

int main(void)
{
    int local_var; // not initialized
    int local_var2 = 0; // best practice: always initialized variables
}